
'use client';

import React from 'react';
import Gallery from '../../../components/Gallery';

export default function GalleryPage({ params }: { params: { shortId: string } }) {
  return <Gallery />;
}
